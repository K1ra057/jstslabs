{
  "compilerOptions": {
    "target": "es5", // Какой стандарт JS использовать
    "module": "commonjs", // Как модули будут подключаться
    "strict": true, // Включить строгий режим, чтобы ошибок было меньше
    "esModuleInterop": true, // Чтобы с другими модулями дружил
    "outDir": "./dist", // Куда складывать скомпилированные файлы
    "rootDir": "./src"   // Где исходники живут
  },
  "include": ["src/**/*", "other-path/**/*"], // Какие файлы включить
  "exclude": ["node_modules"] // Чего игнорить (чтобы node_modules не мешал)
}
